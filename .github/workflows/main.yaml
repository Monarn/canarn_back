name: Update Backend on Raspberry Pi

on:
  push:
    branches:
      - main

jobs:
  update-backend:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: SSH into Raspberry Pi and Update Backend
        run: |
          sshpass -p "${{ secrets.RASPBERRY_PI_PASSWORD }}" \
            ssh -o StrictHostKeyChecking=no romann@90.0.103.81 \
            'cd /var/www/canarn/server/server ; git pull ; sudo systemctl restart canarn-backend'
        env:
          RASPBERRY_PI_PASSWORD: ${{ secrets.RASPBERRY_PI_PASSWORD }}
